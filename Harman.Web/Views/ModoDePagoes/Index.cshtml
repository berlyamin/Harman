@model IEnumerable<Harman.Web.Data.Entities.ModoDePago>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DescripcionModoDePago)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Estado)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DescripcionModoDePago)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Estado.StatusCode)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PaymentTermsID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PaymentTermsID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PaymentTermsID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>

@model IEnumerable<Harman.Web.Data.Entities.TipoDeTransaccion>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TransactionTypecode)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DescripcionTipoDeTransaccion)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TransactionTypecode)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DescripcionTipoDeTransaccion)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TipoDeTransaccionID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TipoDeTransaccionID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TipoDeTransaccionID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>

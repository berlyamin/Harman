@model IEnumerable<Harman.Web.Data.Entities.Unidad>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CodigoUnidad)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NombreUnidad)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CodigoUnidad)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NombreUnidad)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.UnidadID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.UnidadID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.UnidadID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>

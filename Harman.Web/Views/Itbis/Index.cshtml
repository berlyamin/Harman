@model IEnumerable<Harman.Web.Data.Entities.Itbis>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PorcientoItbis)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DescripcionItbis)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PorcientoItbis)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DescripcionItbis)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ItbisID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ItbisID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ItbisID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>

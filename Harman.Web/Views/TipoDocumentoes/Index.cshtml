@model IEnumerable<Harman.Web.Data.Entities.TipoDocumento>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DescripcionTipoDocumento)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DescripcionTipoDocumento)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TipoDocumentoID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TipoDocumentoID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TipoDocumentoID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>

@model IEnumerable<Harman.Web.Data.Entities.Factura>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Fecha)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NumeroPago)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Itbis)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Fecha)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NumeroPago)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Itbis.DescripcionItbis)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.FacturaID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.FacturaID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.FacturaID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
